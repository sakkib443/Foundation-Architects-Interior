# NOTE: vendor, node_modules and public/build are intentionally tracked
# so the project runs identically after a plain `git pull` (no composer/npm install needed).
# .env stays IGNORED — never push secrets to GitHub. Copy it across machines by hand.
*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
# /node_modules             <- tracked on purpose
# /public/build             <- tracked on purpose
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
# /vendor                   <- tracked on purpose
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
